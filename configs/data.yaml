dataset:
  name: squad
  version: "1.1"   # update if needed
split:
  strategy: doc_level
  seed: 42
chunking:
  chunk_size: 400
  chunk_overlap: 80
paths:
  data_dir: data
  processed_dir: data/processed
